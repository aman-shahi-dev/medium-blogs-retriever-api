{
  "name": "medium-blogs-retriever-monorepo",
  "version": "1.0.0",
  "description": "Monorepo to run and build both Medium blog retriever client and server packages.",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client --include=dev",
    "build": "npm run build --prefix client && npx puppeteer browsers install chrome",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
